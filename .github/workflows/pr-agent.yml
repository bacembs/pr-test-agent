name: PR Agent

on:
  pull_request:
    types: [opened, synchronize, reopened]
  issue_comment:

jobs:
  pr_agent_job:
    runs-on: ubuntu-latest
    name: Run PR Agent
    permissions:
      contents: read
      pull-requests: write
      issues: write
    if: ${{ github.event.sender.type != 'Bot' }}
    steps:
      - name: PR Agent action step
        id: pragent
        uses: Codium-ai/pr-agent@main
        env:
          OPENAI_KEY: ${{ secrets.OPENAI_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          config.model: gpt-4o-mini
          config.pr_reviewer.num_max_findings: -1
          config.pr_code_suggestions.num_code_suggestions_per_chunk: -1
          config.pr_description.auto_describe: true
          config.pr_reviewer.auto_review: true
          config.pr_code_suggestions.auto_improve: true
